import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  mainContainer: {
    flex: 1,
    backgroundColor: '#000',
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    color: '#fff',
    fontSize: 20,
    fontWeight: '500',
  },
  btn: {
    marginTop: 20,
    width: '40%',
    height: 30,
    backgroundColor: '#0ff',
    fontSize: 35,
    color: '#000',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },
});
